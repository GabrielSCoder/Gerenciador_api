export type usuarioFormulario = {
    nome: string
    email: string
    senha: string
    perfil_acesso_id : number
}

export type usuarioFormularioResponse = {
    id: number
    nome : string
    email: string
    senha: string
    perfil_acesso_id : number
}