export type usuarioFormulario = {
    nome: string
    email: string
    senha: string
}

export type usuarioFormularioResponse = {
    id: number
    nome : string
    email: string
    senha: string
}